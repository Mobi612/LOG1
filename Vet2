#include <stdio.h>

int main(){
    int A[8] = {1,2,3,4,5,6,7,8};
    int B[8];
    int i;

    for (i = 0; i < 8; i++) {
        B[i] = A[i] * 3;
    }
    printf("Elementos da matriz B\n");
    for (i = 0; i < 8; i++) {
        printf("B[%d] = %d\n", i, B[i]);
    }
    return 0;
}
